# Bootstrap runs with a LOCAL backend on first execution (it creates the
# remote state bucket). No remote backend config needed here. After the bucket
# exists, you may migrate with the gcs block in backends.tf.
